<?xml version="1.0" encoding="utf-8"?>
<menu
	xmlns:android="http://schemas.android.com/apk/res/android"
	xmlns:app="http://schemas.android.com/apk/res-auto">

	<item
		android:id="@+id/action_search"
		android:icon="?actionModeWebSearchDrawable"
		android:title="@string/search"
		app:actionViewClass="androidx.appcompat.widget.SearchView"
		app:showAsAction="ifRoom|collapseActionView" />

	<item
		android:id="@+id/action_filter_extensions"
		android:icon="@drawable/ic_filter_menu"
		android:title="@string/filter"
		app:showAsAction="ifRoom">
		<menu>
			<group android:checkableBehavior="single">
				<item
					android:id="@+id/action_filter_extensions_all"
					android:title="@string/filter_sources_all" />
				<item
					android:id="@+id/action_filter_extensions_updates"
					android:title="@string/updates_pending" />
				<item
					android:id="@+id/action_filter_extensions_installed"
					android:title="@string/installed" />
				<item
					android:id="@+id/action_filter_extensions_available"
					android:title="@string/available_to_install" />
			</group>
		</menu>
	</item>

	<item
		android:id="@+id/action_repo"
		android:icon="@drawable/ic_edit"
		android:title="@string/manage_repo"
		app:showAsAction="ifRoom" />

	<item
		android:id="@+id/action_repo_remove"
		android:icon="@drawable/ic_delete"
		android:title="@string/remove_repo"
		app:showAsAction="never" />

</menu>
